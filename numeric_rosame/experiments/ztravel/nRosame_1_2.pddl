(define (domain zeno-travel)
(:requirements :typing :fluents :negative-preconditions :equality)
(:types 	aircraft person - movable
	movable city - object
)

(:predicates (at ?x - movable ?c - city)
	(in ?p - person ?a - aircraft)
)

(:functions (fuel ?a - aircraft)
	(distance ?c1 - city ?c2 - city)
	(slow-burn ?a - aircraft)
	(fast-burn ?a - aircraft)
	(capacity ?a - aircraft)
	(total-fuel-used )
	(onboard ?a - aircraft)
	(zoom-limit ?a - aircraft)
)

(:action board
	:parameters (?a - person ?b - aircraft ?c - city)
	:precondition (and (at ?a ?c)
	(at ?b ?c)
	(= (capacity ?b) 8062)
	(= (fast-burn ?b) 11)
	(= (fuel ?b) 5350)
	(= (onboard ?b) 0)
	(= (slow-burn ?b) 3)
	(= (total-fuel-used ) 2712)
	(= (zoom-limit ?b) 3))
	:effect (and (in ?a ?b)
		(not (at ?a ?c))
(assign (onboard ?b) 1)))

(:action debark
	:parameters (?a - person ?b - aircraft ?c - city)
	:precondition (and (at ?b ?c)
	(in ?a ?b)
	(= (capacity ?b) 8062)
	(= (fast-burn ?b) 11)
	(= (fuel ?b) 3718)
	(= (onboard ?b) 1)
	(= (slow-burn ?b) 3)
	(= (total-fuel-used ) 4344)
	(= (zoom-limit ?b) 3))
	:effect (and (at ?a ?c)
		(not (in ?a ?b))
(assign (onboard ?b) 0)))

(:action fly
	:parameters (?a - aircraft ?b - city ?c - city)
	:precondition (and (at ?a ?b)
	(<= (* (onboard ?a) -1) 0)
	(<= (onboard ?a) 1)
	(= (+ (* (distance ?b ?c) 1) (+ (* (total-fuel-used ) 0.1304) (* (distance ?c ?b) -0.0173))) 888.3466)
	(= (+ (* (distance ?c ?b) 1) (* (total-fuel-used ) 0.1327)) 904)
	(= (+ (* (fuel ?a) 1) (+ (* (total-fuel-used ) 0.9660) (+ (* (onboard ?a) 0.0004) (+ (* (distance ?b ?c) -0.1282) (* (distance ?c ?b) -0.1282))))) 7830.1701)
	(= (capacity ?a) 8062)
	(= (fast-burn ?a) 11)
	(= (slow-burn ?a) 3)
	(= (total-fuel-used ) (* (onboard ?a) 2712))
	(= (zoom-limit ?a) 3))
	:effect (and (at ?a ?c)
		(not (at ?a ?b))
(decrease (fuel ?a) (+ (* (fuel ?a) 0.7017) (+ (* (distance ?b ?c) -0.0396) (+ (* (total-fuel-used ) 0.2983) -2909.6598))))		
(increase (total-fuel-used ) (+ (* (fuel ?a) -0.2983) (+ (* (distance ?b ?c) -0.0396) (+ (* (total-fuel-used ) -0.7017) 5152.3402))))))

(:action refuel
	:parameters (?a - aircraft ?b - city)
	:precondition (and (at ?a ?b)
	(= (capacity ?a) 8062)
	(= (fast-burn ?a) 11)
	(= (fuel ?a) 199)
	(= (onboard ?a) 0)
	(= (slow-burn ?a) 3)
	(= (total-fuel-used ) 0)
	(= (zoom-limit ?a) 3))
	:effect (and 
(assign (fuel ?a) 8062)))

)